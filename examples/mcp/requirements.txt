# tracesage MCP examples — dependencies for all demos in examples/mcp/.
#
# Install:
#   pip install -r examples/mcp/requirements.txt
#
# API key (set whichever you have):
#   export ANTHROPIC_API_KEY=...       # default
#   export OPENROUTER_API_KEY=...      # or use OpenRouter
#
# Gmail auth (one-time, gmail_youtube_demo only):
#   uv tool install mcp-google-gmail
#   mcp-google-gmail auth

# --- tracesage ---
tracesage[mcp]
python-dotenv

# --- LangChain stack ---
langchain>=0.3
langgraph>=0.3
langchain-anthropic>=0.2
langchain-openai>=0.2

# --- external MCP servers (gmail_youtube_demo) ---
mcp-google-gmail
mcp-youtube-transcript
